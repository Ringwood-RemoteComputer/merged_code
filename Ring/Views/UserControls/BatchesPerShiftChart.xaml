<UserControl x:Class="Ring.Views.UserControls.BatchesPerShiftChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             Background="Transparent">
    <Grid x:Name="ChartContainer" Margin="10">
        <Border Background="#F8F9FA" 
                CornerRadius="8" 
                BorderThickness="1" 
                BorderBrush="#E0E0E0"
                Padding="15">
            <lvc:CartesianChart Series="{Binding BatchesPerShiftSeries}" 
                               LegendLocation="None"
                               Background="Transparent"
                               Hoverable="True"
                               DataTooltip="{x:Null}">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Shift" 
                             Labels="{Binding ShiftLabels}"
                             LabelsRotation="45"
                             Foreground="#1E1E1E"
                             FontSize="11"
                             FontWeight="SemiBold">
                        <lvc:Axis.Separator>
                            <lvc:Separator StrokeThickness="1" 
                                          StrokeDashArray="2,2" 
                                          Stroke="#E0E0E0"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="Number of Batches" 
                             MinValue="0"
                             MaxValue="15"
                             Foreground="#1E1E1E"
                             FontSize="11"
                             FontWeight="SemiBold">
                        <lvc:Axis.Separator>
                            <lvc:Separator StrokeThickness="1" 
                                          StrokeDashArray="2,2" 
                                          Stroke="#E0E0E0"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </Border>
    </Grid>
</UserControl>
