<Window x:Class="Ring.Views.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Password" Height="200" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5"
        WindowStyle="SingleBorderWindow">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Password Type Dropdown -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Password Type:" 
                       FontWeight="SemiBold" 
                       FontSize="12" 
                       Margin="0,0,0,5"
                       Foreground="#1E1E1E"/>
            <ComboBox x:Name="PasswordTypeComboBox"
                      SelectedIndex="0"
                      Height="30"
                      FontSize="12"
                      Padding="8,0">
                <ComboBoxItem Content="Supervisor"/>
                <ComboBoxItem Content="Admin"/>
            </ComboBox>
        </StackPanel>

        <!-- Password Input -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Password:" 
                       FontWeight="SemiBold" 
                       FontSize="12" 
                       Margin="0,0,0,5"
                       Foreground="#1E1E1E"/>
            <PasswordBox x:Name="PasswordBox"
                         Height="30"
                         FontSize="14"
                         Padding="8,0"
                         PasswordChar="*"
                         KeyDown="PasswordBox_KeyDown"/>
        </StackPanel>

        <!-- Error Message -->
        <TextBlock x:Name="ErrorMessageText"
                   Grid.Row="2"
                   Foreground="#DC3545"
                   FontSize="11"
                   Margin="0,0,0,10"
                   Visibility="Collapsed"
                   TextWrapping="Wrap"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="OK" 
                    Width="80" 
                    Height="32"
                    Margin="0,0,10,0"
                    Click="OkButton_Click"
                    Style="{StaticResource ActionButtonStyle}"/>
            <Button Content="Cancel" 
                    Width="80" 
                    Height="32"
                    Click="CancelButton_Click"
                    Style="{StaticResource CloseButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>

