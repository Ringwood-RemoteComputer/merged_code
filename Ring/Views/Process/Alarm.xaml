<UserControl x:Class="Ring.Views.Process.Alarm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Main content area -->
        <Grid Grid.Row="0" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <!-- Alarm Management Table -->
            <Border Grid.Column="0" 
                    BorderThickness="1" 
                    BorderBrush="#E0E0E0" 
                    CornerRadius="8" 
                    Margin="0,0,16,0"
                    Background="White"
                    Effect="{StaticResource DropShadowEffect}">
                <Grid Margin="24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Section Header -->
                    <Border Grid.Row="0" 
                            BorderThickness="1" 
                            BorderBrush="#E0E0E0" 
                            CornerRadius="6" 
                            Margin="0,0,0,16"
                            Background="White">
                        <StackPanel Margin="16">
                            <TextBlock Text="Alarm Management" 
                                       FontWeight="SemiBold" 
                                       FontSize="16" 
                                       HorizontalAlignment="Center" 
                                       Margin="0,0,0,0" 
                                       Foreground="{StaticResource PrimaryBrush}" 
                                       LineHeight="20" />
                        </StackPanel>
                    </Border>

                    <!-- DataGrid -->
                    <DataGrid Grid.Row="1" 
                              Name="AlarmDataGrid"
                              AutoGenerateColumns="False" 
                              IsReadOnly="True"
                              Background="White"
                              BorderBrush="#E0E0E0"
                              Foreground="#1E1E1E"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              CanUserResizeColumns="True"
                              CanUserSortColumns="True"
                              AlternatingRowBackground="#F8F9FA"
                              RowBackground="White"
                              HorizontalGridLinesBrush="#E0E0E0"
                              VerticalGridLinesBrush="Transparent"
                              FontSize="14"
                              RowHeight="32">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Type" Width="*" 
                                                Binding="{Binding Type}"
                                                FontSize="14">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="ToolTip" Value="Type" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Status" Width="*" 
                                                Binding="{Binding Status}"
                                                FontSize="14">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="ToolTip" Value="Status" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Alarm Number" Width="*" 
                                                Binding="{Binding AlarmNumber}"
                                                FontSize="14">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="ToolTip" Value="Alarm Number" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Alarm Description" Width="3*" 
                                                Binding="{Binding AlarmDescription}"
                                                FontSize="14">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="ToolTip" Value="Alarm Description" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Alarm Date" Width="*" 
                                                Binding="{Binding AlarmDate}"
                                                FontSize="14">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="ToolTip" Value="Alarm Date" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Alarm Time" Width="*" 
                                                Binding="{Binding AlarmTime}"
                                                FontSize="14">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="ToolTip" Value="Alarm Time" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Acknowledge Date" Width="*" 
                                                Binding="{Binding AcknowledgeDate}"
                                                FontSize="14">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="ToolTip" Value="Acknowledge Date" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Acknowledge Time" Width="*" 
                                                Binding="{Binding AcknowledgeTime}"
                                                FontSize="14">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="ToolTip" Value="Acknowledge Time" />
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Right Panel -->
            <Border Grid.Column="1" 
                    BorderThickness="1" 
                    BorderBrush="#E0E0E0" 
                    CornerRadius="8" 
                    Margin="16,0,0,0"
                    Background="White"
                    Effect="{StaticResource DropShadowEffect}">
                <StackPanel Margin="24">
                    <!-- Visible Data Range -->
                    <Border BorderThickness="1" 
                            BorderBrush="#E0E0E0" 
                            CornerRadius="6" 
                            Margin="0,0,0,20"
                            Background="White">
                        <StackPanel Margin="16">
                            <TextBlock Text="Visible Data Range" 
                                       FontWeight="SemiBold" 
                                       FontSize="14" 
                                       HorizontalAlignment="Center" 
                                       Margin="0,0,0,16" 
                                       Foreground="{StaticResource PrimaryBrush}" 
                                       LineHeight="18" />
                            
                            <StackPanel Margin="0,0,0,12">
                                <TextBlock Text="Start Date:" 
                                           Margin="0,0,0,8"
                                           Foreground="#1E1E1E" 
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           LineHeight="18" />
                                <DatePicker Name="StartDatePicker" 
                                           Width="150" 
                                           FontSize="14"
                                           Padding="8,6" />
                            </StackPanel>
                            
                            <StackPanel Margin="0,0,0,0">
                                <TextBlock Text="End Date:" 
                                           Margin="0,0,0,8"
                                           Foreground="#1E1E1E" 
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           LineHeight="18" />
                                <DatePicker Name="EndDatePicker" 
                                           Width="150" 
                                           FontSize="14"
                                           Padding="8,6" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Alarm Actions -->
                    <Border BorderThickness="1" 
                            BorderBrush="#E0E0E0" 
                            CornerRadius="6" 
                            Margin="0,0,0,0"
                            Background="White">
                        <StackPanel Margin="16">
                            <TextBlock Text="Alarm Actions" 
                                       FontWeight="SemiBold" 
                                       FontSize="14" 
                                       HorizontalAlignment="Center" 
                                       Margin="0,0,0,16" 
                                       Foreground="{StaticResource PrimaryBrush}" 
                                       LineHeight="18" />
                            
                            <Button Content="ALARM SILENCE" 
                                    Width="180" 
                                    Height="40" 
                                    Margin="0,0,0,8"
                                    FontWeight="SemiBold"
                                    FontSize="12"
                                    Style="{StaticResource ActionButtonStyle}"
                                    Click="AlarmSilence_Click" />
                            <Button Content="EXPORT" 
                                    Width="180" 
                                    Height="40" 
                                    Margin="0,0,0,0"
                                    FontWeight="SemiBold"
                                    FontSize="12"
                                    Style="{StaticResource CloseButtonStyle}"
                                    Click="Export_Click" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Footer Control Bar -->
        <Border Grid.Row="1" 
                BorderThickness="0,1,0,0" 
                BorderBrush="#E0E0E0" 
                Background="White"
                Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>
                
                <!-- Left side - Alarm Status -->
                <StackPanel Grid.Column="0" 
                            Orientation="Horizontal" 
                            VerticalAlignment="Center">
                    <TextBlock Text="Active Alarms: " 
                               Foreground="#1E1E1E"
                               FontSize="14"
                               FontWeight="SemiBold" />
                    <TextBlock Name="ActiveAlarmCountText" 
                               Text="0" 
                               Foreground="{StaticResource PrimaryBrush}"
                               FontSize="14"
                               FontWeight="Bold" />
                </StackPanel>
                
                <!-- Middle - Empty space -->
                <StackPanel Grid.Column="1" />
                
                <!-- Right side - Close button -->
                <Button Grid.Column="2" 
                        Content="CLOSE" 
                        Width="120"
                        Height="40"
                        HorizontalAlignment="Right"
                        Style="{StaticResource CloseButtonStyle}"
                        Click="Close_Click" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
